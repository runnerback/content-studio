---
title: Automated review fixes
---
- Rednote theme editor and settings: collapsible sections and the sample-template dropdown now toggle CSS classes instead of writing inline `display`; section headings use `Setting.setHeading()`.
- Production build is minified (same as the official sample plugin): `main.js` drops from 5.0 MB to about 3.8 MB, under the Obsidian Sync Standard 5 MB limit.
- `LICENSE` is now the plain MIT text so GitHub and the directory recognise it; upstream copyright notices moved to `THIRD_PARTY_NOTICES.md`.
