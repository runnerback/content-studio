---
title: ZIP folder named after the note
---
- 「导出全部页」: the top-level folder inside the ZIP is now named after the note (same as the ZIP file) instead of a fixed `export-to-rednote/`, so several extracted exports can be told apart by folder name.
