---
title: Code quality pass against the directory scanner
---
- Reproduced the Obsidian community directory scan locally (`npm run scan:directory`, obsidianmd recommended + type-aware rules) and worked through it: total findings 4987 → 1498, `no-unsafe-call` 1179 → 185.
- Rednote (小红书) module: Obsidian DOM helpers instead of `document.createElement`, `window.*` timers, `setIcon` instead of `innerHTML` SVGs, no Node `events` dependency, typed theme style sections (was `any`).
- Plugin views: `this` inside the view mixins is now typed (`types/view-mixins.d.ts`, generated by `npm run generate:view-types` and verified in `pretest`); view members declared in the constructor; the Obsidian module is typed instead of `any`.
- Removed lifecycle `console.log`s; bridge diagnostics moved to `console.debug`.
- No functional changes.
