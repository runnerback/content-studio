---
title: Replace jszip with fflate
---
- Card ZIP export now uses `fflate` instead of `jszip`. jszip bundled a legacy setImmediate polyfill that creates `<script>` elements, which the Obsidian automated review flags as runtime script injection. Bundle shrinks by about 150 KB.
